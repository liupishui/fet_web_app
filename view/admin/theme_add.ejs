<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="utf-8">
    <meta name="viewport"
        content="width=device-width,initial-scale=1.0, user-scalable=0, minimum-scale=1.0, maximum-scale=1.0">
    <title>管理中心</title>
    <link href="static/css/bootstrap.min14ed.css?v=3.3.6" rel="stylesheet">
    <link href="static/css/font-awesome.min93e3.css?v=4.4.0" rel="stylesheet">
    <link href="static/css/plugins/bootstrap-table/bootstrap-table.min.css" rel="stylesheet">
    <link href="static/css/animate.min.css" rel="stylesheet">
    <link href="static/css/style.min862f.css?v=4.1.0" rel="stylesheet">
</head>

<body class="gray-bg">
    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-sm-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>用户主题添加</h5>
                        <div class="ibox-tools">
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                            <a class="dropdown-toggle" data-toggle="dropdown" href="form_basic.html#">
                                <i class="fa fa-wrench"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-user">
                                <li><a href="form_basic.html#">选项1</a>
                                </li>
                                <li><a href="form_basic.html#">选项2</a>
                                </li>
                            </ul>
                            <a class="close-link">
                                <i class="fa fa-times"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">
                            <div class="form-horizontal m-t">
                                <div class="form-group">
                                    <label for="" class="col-sm-3 control-label">是否内置主题：</label>
                                    <div class="col-sm-8">
                                        <input type="checkbox" <%- is_system_theme?'checked':'' %> id="is_system_theme" class="checkbox-inline">
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="" class="col-sm-3 control-label">是否用户默认主题：</label>
                                    <div class="col-sm-8">
                                        <input type="checkbox" <%- is_default?'checked':'' %> id="is_default" class="checkbox-inline">
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="" class="col-sm-3 control-label">主题名字：</label>
                                    <div class="col-sm-8">
                                        <input class="form-control" id="theme_name" value="<%- modify!==-1 ? theme_name:'' -%>" placeholder="请输入主题名" type="text" />
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="" class="col-sm-3 control-label">封面：</label>
                                    <div class="col-sm-8">
                                        <div class="form-control" style="border:none;height:auto;font-size:0;line-height:0;">
                                            <img src="<%- modify!==-1 ? poster:'' -%>" style="width:100px;"/>
                                        </div>
                                        <label for="avatar" id="poster" class="btn-add">
                                            <span class="btn btn-primary">点击上传</span>
                                        </label>
                                        <span class="help-block m-b-none"><i class="fa fa-info-circle"></i>封面</span>                                               
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="" class="col-sm-3 control-label">主题根路径：</label>
                                    <div class="col-sm-8">
                                        <input type="text" class="form-control" id="theme_server_path" value="<%- modify!==-1 ? theme_server_path:'' -%>" placeholder="请输入主题根路径" />
                                        <span class="help-block m-b-none"><i class="fa fa-info-circle"></i> 例如：/view/theme/default</span>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="" class="col-sm-3 control-label">入口页面：</label>
                                    <div class="col-sm-8">
                                        <input type="text" class="form-control" id="entry_page" value="<%- modify!==-1 ? entry_page:'' -%>" placeholder="请输入主题入口页面" />
                                        <span class="help-block m-b-none"><i class="fa fa-info-circle"></i> 默认index.ejs,入口页面可以为index.html,main.html,default.html等</span>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="col-sm-8 col-sm-offset-3">
                                        <button class="btn btn-primary btn-submit" type="button">提交</button>
                                    </div>
                                </div>
                            </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
<script type="text/javascript" src="static/js/jquery.min.js"></script>
<script src="static/js/plugins/layer/layer.min.js" type="text/javascript"></script>
<script type="text/javascript" src="scripts/fcup/fcup.min.js"></script>
<script src="/admin/scripts/common.js" type="text/javascript"></script>

<script type="text/javascript">
    $(function(){
        uploadImgFcup('poster');
        <% if(modify===-1){ %>
        $(".btn-submit").click(function(){
            if($.trim($("#theme_name").val())==''){
                layer.msg('请输入主题名字');
                return;
            }
            if($.trim($("#theme_server_path").val())==''){
                layer.msg('请输入主题路径');
                return;
            }
            // if(typeof($("#poster").prev().find('img').attr('src'))=='undefined'||$("#avatar").prev().find('img').attr('src')==''){
            //     layer.msg('请上传封面图片');
            //     return;
            // }
            $.post('/admin/theme_add',{
                'type':'insert',
                'is_system_theme':$("#is_system_theme").is(':checked')?1:0,
                'is_default':$("#is_default").is(':checked')?1:0,
                'theme_name':$("#theme_name").val(),
                'poster':$("#poster").prev().find('img').attr('src') && $("#poster").prev().find('img').attr('src')!==''?$("#poster").prev().find('img').attr('src').split('?')[0]:'',
                'theme_server_path':$("#theme_server_path").val(),
                'entry_page':$("#entry_page").val()
            },function(data){
                if(data=='路径不存在'){
                    layer.msg('路径不存在');
                }
                if(data==1){
                    layer.confirm('添加成功',{btn:['继续添加','返回列表']},function(index){
                        window.location.reload();
                    },function (index) {
                        window.top.openMenu($(window.top.document).find('[data-index=20230804]'));
                        window.top.freshThemeList();
                        window.location.reload();
                    })
                }
            })
        });
        <%} else {%>
            $(".btn-submit").click(function(){
                if($.trim($("#theme_name").val())==''){
                    layer.msg('请输入主题名字');
                    return;
                }
                if($.trim($("#theme_server_path").val())==''){
                    layer.msg('请输入主题路径');
                    return;
                }
                // if(typeof($("#poster").prev().find('img').attr('src'))=='undefined'||$("#avatar").prev().find('img').attr('src')==''){
                //     layer.msg('请上传封面图片');
                //     return;
                // }
                $.post('/admin/theme_add',{
                    'type':'modify',
                    'modify':'<%-modify%>',
                    'is_system_theme':$("#is_system_theme").is(':checked')?1:0,
                    'is_default':$("#is_default").is(':checked')?1:0,
                    'theme_name':$("#theme_name").val(),
                    'poster':$("#poster").prev().find('img').attr('src') && $("#poster").prev().find('img').attr('src')!==''?$("#poster").prev().find('img').attr('src').split('?')[0]:'',
                    'theme_server_path':$("#theme_server_path").val(),
                    'entry_page':$("#entry_page").val()
                },function(data){
                    if(data=='路径不存在'){
                        layer.msg('路径不存在');
                    }
                    if(data==1){
                        window.top.freshThemeList();
                        var index = window.top.layer.getFrameIndex(window.name);
                        window.top.layer.close(index);
                    }
                })
            });
        <% } %>
    })
</script>
</html>