class indexClass {
    constructor(context,app,param,setup) {
        this.context = context;
        this.app = app;
        this.param = param;
        this.setup = setup;
    }
    async run(){
    }
}
module.exports = indexClass;